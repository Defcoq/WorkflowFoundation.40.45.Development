namespace Mvc.Wwf.Membership.Registration.Activities
{
    public static class RegistrationConstants
    {
        #region Constants and Fields

        public static readonly string Cancel = RegistrationCommand.Cancel.ToString();

        public static readonly string Confirm = RegistrationCommand.Confirm.ToString();

        public static readonly string SendMail = RegistrationCommand.SendMail.ToString();

        #endregion
    }
}